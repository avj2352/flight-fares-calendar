/**
 * Helper middleware before and after Test scenario creation. 
 * Currently just returning true to have placeholders
 */
module.exports.before = async () => {    
    return true;
};
  
module.exports.after = async () => {    
    return true;
};